meta {
  name: TC-USER-005 Empty Update Request
  type: http
  seq: 3
}

put {
  url: {{base_url}}/api/{{api_version}}/users/me
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "",
    "phone": ""
  }
}

tests {
  test("Status is 400", function() {
    expect(res.status).to.equal(400);
  });
  
  test("Returns validation error", function() {
    expect(res.body.error).to.include("at least one field");
  });
}
