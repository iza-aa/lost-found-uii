meta {
  name: TC-USER-002 Get Current User
  type: http
  seq: 2
}

get {
  url: {{base_url}}/api/{{api_version}}/users/me
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("Status is 200", function() {
    expect(res.status).to.equal(200);
  });
}
