meta {
  name: TC-USER-003 Get User By ID
  type: http
  seq: 3
}

get {
  url: {{base_url}}/api/{{api_version}}/users/{{user_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("Status is 200", function() {
    expect(res.status).to.equal(200);
  });
}
