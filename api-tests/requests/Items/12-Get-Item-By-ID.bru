meta {
  name: TC-ITEM-12 Get Item By ID
  type: http
  seq: 12
}

get {
  url: {{base_url}}/api/{{api_version}}/items/{{found_item_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("Status is 200", function() {
    expect(res.status).to.equal(200);
  });
}
