meta {
  name: TC-ITEM-16 Get All Lost Items
  type: http
  seq: 16
}

get {
  url: {{base_url}}/api/{{api_version}}/items?type=LOST
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("Status is 200", function() {
    expect(res.status).to.equal(200);
  });
}
