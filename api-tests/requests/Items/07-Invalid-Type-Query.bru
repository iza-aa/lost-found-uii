meta {
  name: TC-ITEM-017 Invalid Type Parameter
  type: http
  seq: 7
}

get {
  url: {{base_url}}/api/{{api_version}}/items?type=STOLEN
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("Status is 400", function() {
    expect(res.status).to.equal(400);
  });
  
  test("Returns type error", function() {
    expect(res.body.error).to.include("type");
  });
}
