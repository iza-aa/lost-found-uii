meta {
  name: TC-ITEM-005 Missing Required Fields
  type: http
  seq: 5
}

post {
  url: {{base_url}}/api/{{api_version}}/items/found
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "title": ""
  }
}

tests {
  test("Status is 400", function() {
    expect(res.status).to.equal(400);
  });
}
