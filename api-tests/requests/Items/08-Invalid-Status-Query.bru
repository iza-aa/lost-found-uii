meta {
  name: TC-ITEM-019 Invalid Status Parameter
  type: http
  seq: 8
}

get {
  url: {{base_url}}/api/{{api_version}}/items?status=PENDING
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("Status is 400", function() {
    expect(res.status).to.equal(400);
  });
  
  test("Returns status error", function() {
    expect(res.body.error).to.include("status");
  });
}
