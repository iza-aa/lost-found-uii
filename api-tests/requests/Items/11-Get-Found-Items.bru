meta {
  name: TC-ITEM-11 Get All Found Items
  type: http
  seq: 11
}

get {
  url: {{base_url}}/api/{{api_version}}/items?type=FOUND
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("Status is 200", function() {
    expect(res.status).to.equal(200);
  });
}
