meta {
  name: TC-AUTH-010 Login Missing Fields
  type: http
  seq: 10
}

post {
  url: {{base_url}}/api/{{api_version}}/auth/login
  body: json
  auth: none
}

body:json {
  {
    "email": "21523120@students.uii.ac.id"
  }
}

tests {
  test("Status is 400", function() {
    expect(res.status).to.equal(400);
  });
}
