meta {
  name: TC-CLAIM-007 Decide Already Processed
  type: http
  seq: 7
}

put {
  url: {{base_url}}/api/{{api_version}}/claims/{{claim_id}}/decide
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "status": "REJECTED"
  }
}

tests {
  test("Status is 400", function() {
    expect(res.status).to.equal(400);
  });
}
