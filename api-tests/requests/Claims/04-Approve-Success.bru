meta {
  name: TC-CLAIM-004 Approve Claim Success
  type: http
  seq: 4
}

put {
  url: {{base_url}}/api/{{api_version}}/claims/{{claim_id}}/decide
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "status": "APPROVED"
  }
}

tests {
  test("Status is 200", function() {
    expect(res.status).to.equal(200);
  });
}
