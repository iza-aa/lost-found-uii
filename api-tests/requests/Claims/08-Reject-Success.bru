meta {
  name: TC-CLAIM-008 Reject Claim Success
  type: http
  seq: 8
}

put {
  url: {{base_url}}/api/{{api_version}}/claims/{{claim_id}}/decide
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "status": "REJECTED"
  }
}

tests {
  test("Status is 200", function() {
    expect(res.status).to.equal(200);
  });
}
