meta {
  name: TC-ENUM-002 Get All Locations
  type: http
  seq: 2
}

get {
  url: {{base_url}}/api/{{api_version}}/enumerations/locations
  body: none
  auth: none
}

tests {
  test("Status is 200", function() {
    expect(res.status).to.equal(200);
  });
}
