meta {
  name: TC-ENUM-005 Delete Category
  type: http
  seq: 5
}

delete {
  url: {{base_url}}/api/{{api_version}}/enumerations/categories/{{category_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("Status is 200", function() {
    expect(res.status).to.equal(200);
  });
}
