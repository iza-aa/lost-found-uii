meta {
  name: TC-ENUM-003 Create Category
  type: http
  seq: 3
}

post {
  url: {{base_url}}/api/{{api_version}}/enumerations/categories
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "New Category"
  }
}

tests {
  test("Status is 200", function() {
    expect(res.status).to.equal(200);
  });
}
