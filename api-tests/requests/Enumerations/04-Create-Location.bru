meta {
  name: TC-ENUM-004 Create Location
  type: http
  seq: 4
}

post {
  url: {{base_url}}/api/{{api_version}}/enumerations/locations
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "New Location"
  }
}

tests {
  test("Status is 200", function() {
    expect(res.status).to.equal(200);
  });
}
