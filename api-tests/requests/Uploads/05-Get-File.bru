meta {
  name: TC-UPLOAD-005 Get Uploaded File
  type: http
  seq: 5
}

get {
  url: {{base_url}}/api/{{api_version}}/uploads/example.jpg
  body: none
  auth: none
}

tests {
  test("Status is 200 or 404", function() {
    expect([200, 404]).to.include(res.status);
  });
}

docs {
  Replace 'example.jpg' with a filename returned from a successful upload.
}
