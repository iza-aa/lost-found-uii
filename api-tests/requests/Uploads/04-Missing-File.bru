meta {
  name: TC-UPLOAD-004 Missing File
  type: http
  seq: 4
}

post {
  url: {{base_url}}/api/{{api_version}}/upload
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:multipart-form {
  
}

tests {
  test("Status is 400", function() {
    expect(res.status).to.equal(400);
  });
}
